- id: rules-check
  name: rules check (.claude/rules drift)
  entry: mutation-gate rules check
  language: python
  always_run: true
  pass_filenames: false
- id: mutation-gate
  name: mutation gate (diff-scoped)
  entry: mutation-gate --staged
  language: python
  always_run: true
  pass_filenames: false
