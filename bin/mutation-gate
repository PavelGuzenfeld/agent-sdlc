#!/usr/bin/env python3
"""Shim so pre-commit and the Stop hook can call the pack by name on PATH."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

from mutation_gate.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
